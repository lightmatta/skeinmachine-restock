# Runtime data (SQLite database + WAL files) — never commit
/data/*.sqlite
/data/*.sqlite-wal
/data/*.sqlite-shm

# Local server-side configuration containing the superuser hash
/config/config.php

# OS / editor noise
.DS_Store
*.log
